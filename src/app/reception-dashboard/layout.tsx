import ReceptionShell from "./ReceptionShell";

export default function ReceptionDashboardLayout({ children }: { children: React.ReactNode }) {
  return <ReceptionShell>{children}</ReceptionShell>;
}
